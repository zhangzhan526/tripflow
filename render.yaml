services:
  - type: web
    name: tripflow-pwa-beta
    env: docker
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: PORT
        value: 5173
      - key: NODE_ENV
        value: production
